{
  "name": "@promptdeploy/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@promptdeploy/shared-types": "workspace:*"
  }
}
